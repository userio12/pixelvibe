enum ViewMode { folder, tree, albums }

enum LayoutMode { list, grid }

enum DisplayField {
  fullName,
  path,
  totalVideos,
  totalDuration,
  folderSize,
  date;
}

enum SortMode { name, date, size, type }

enum MediaTypeFilter { all, video, playlist }
